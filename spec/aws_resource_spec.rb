require 'spec_helper'

describe AwsResource do
  it 'has a version number' do
    expect(AwsResource::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
